# PyTorch: default wheels come from PyPI. For a specific backend, install torch first, e.g.
#   CPU:    pip install torch --index-url https://download.pytorch.org/whl/cpu
#   CUDA:   https://pytorch.org/get-started/locally/
#   ROCm:   pip install torch --index-url https://download.pytorch.org/whl/rocm6.2
# Then: pip install -r requirements.txt
torch>=2.1.0
transformers>=4.40.0
datasets>=2.16.0
peft>=0.11.0
trl>=0.8.0
accelerate>=0.30.0
sentence-transformers>=2.6.0
faiss-cpu>=1.7.4
numpy>=1.24.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
requests>=2.31.0
pypdf>=4.0.0
sentencepiece
evaluate

# Optional CUDA-only acceleration. Install after torch in a compatible CUDA env:
#   pip install flash-attn --no-build-isolation
# Keep it out of the base requirements because its build step imports torch.
